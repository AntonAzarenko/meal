<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.azarenka.UserRepository">

    <resultMap type="User" id="user">
        <id column="id" property="id"/>
        <result column="email" property="email"/>
        <result column="enabled" property="enabled"/>
        <result column="name" property="name"/>
        <result column="password" property="password"/>
        <result column="activateCode" property="active_code"/>
        <result column="registrationDate" property="date_registration"/>

        <collection property="roles" ofType="com.azarenka.Role" javaType="java.util.HashSet">
            <result property="name" column="role"/>
        </collection>
    </resultMap>

    <insert id="save" parameterType="User">
        INSERT INTO home.users (
            id,
            email,
            enabled,
            name,
            avatar,
            password,
            activate_code,
            date_registration
        )
        VALUES (
            #{id},
            #{email},
            #{enabled},
            #{name},
            #{avatar},
            #{password},
            #{activateCode},
            #{registrationDate}
        )
    </insert>
    
    <update id="update">
        UPDATE  home.users SET enabled = #{enabled}
        where home.users.id = #{id}
    </update>

    <select id="getByEmail" resultMap="user">
        SELECT
            home.users.id,
            email,
            name,
            avatar,
            password,
            r.id,
            r.role,
            enabled
        FROM
            home.users
            JOIN home.user_role_map rm ON rm.user_id = id
            JOIN home.role r ON r.id = rm.role_id
        WHERE home.users.email = #{email}
    </select>

    <select id="getByActivateCode" resultMap="user">
        SELECT
        home.users.id,
        email,
        name,
        avatar,
        password,
        r.id,
        r.role,
        enabled
        FROM
        home.users
        JOIN home.user_role_map rm ON rm.user_id = id
        JOIN home.role r ON r.id = rm.role_id
        WHERE home.users.activate_code = #{code}
    </select>
</mapper>