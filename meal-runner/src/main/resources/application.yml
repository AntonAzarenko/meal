server:
  servlet:
    context-path: /api

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: root
  liquibase:
    change-log: classpath:liquibase/changelog.xml
    password: root
    url: jdbc:postgresql://localhost:5432/postgres
    user: postgres
  http:
    enabled: true
    force: true
